rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload and read blog thumbnails
    match /blog-thumbnails/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
